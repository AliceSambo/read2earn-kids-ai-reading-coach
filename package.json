{
  "name": "read2earn-kids-ai-reading-coach",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "A child-safe Living Reading Mission prototype.",
  "scripts": {
    "start": "node server.mjs",
    "check": "node --check server.mjs && node --check public/app.js && node --check public/design/tokens.js && node --check public/design/components.js",
    "test": "npm run check && node scripts/check-foundation.mjs && node scripts/check-design-system.mjs",
    "test:visual": "node scripts/visual-smoke.mjs"
  },
  "engines": {
    "node": ">=20"
  },
  "devDependencies": {
    "playwright-core": "^1.55.0"
  }
}
